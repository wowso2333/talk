# spring.application.name=demo

# MySQL local

# railway only!!
#spring.datasource.url=jdbc:mysql://mysql.railway.internal:3306/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.url=jdbc:mysql://db:3306/talks
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ??mysql?????
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.initialization-mode=always
spring.datasource.hikari.maxLifetime=600000
spring.datasource.hikari.idleTimeout=30000

spring.security.user.name=tina
spring.security.user.password=wang

# MyBatis config
mybatis.mapper-locations=classpath:dao/*.xml
mybatis.type-aliases-package=com.talks.demo.articleDao.pojo

# ????????
mybatis.configuration.map-underscore-to-camel-case=true

spring.data.redis.host=my-redis
#spring.data.redis.host=redis.railway.internal
spring.data.redis.port=6379
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.ssl.enabled=false
spring.data.redis.connect-timeout=3s
spring.data.redis.timeout=3s
# Cache avalanche protection: add random extra TTL to avoid many keys expiring together.
cache.ttl-jitter-percent=20
# Redis
logging.level.org.springframework.data.redis=DEBUG

#??Railway?PORT????
server.address=0.0.0.0
server.port=${PORT:8080}

logging.level.root=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.com.talks.demo=DEBUG

# DevTools
spring.devtools.restart.enabled=true
spring.devtools.restart.additional-paths=src/main/java,src/main/resources
spring.devtools.restart.poll-interval=2s
spring.devtools.restart.quiet-period=1s

# Image upload limits.
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB


jwt.secret=${JWT_SECRET}

# aws and my-redis secret in .env
# Cloudflare R2：沿用 AWS S3 SDK v2，但改用 R2 endpoint 與環境變數。
r2.access-key-id=${R2_ACCESS_KEY_ID}
r2.secret-access-key=${R2_SECRET_ACCESS_KEY}
r2.endpoint=${R2_ENDPOINT}
r2.bucket=${R2_BUCKET}
r2.region=${R2_REGION:auto}
# R2 公開讀取網址，供前端 img src 使用；例如 https://xxxx.r2.dev
r2.public-url=${R2_PUBLIC_URL}

# 舊 AWS S3 設定目前已不再被程式讀取，確認 R2 上線後可再刪除。
# cloud.aws.credentials.accessKey=${AWS_ACCESS_KEY_ID}
# cloud.aws.credentials.secretKey=${AWS_SECRET_ACCESS_KEY}
# cloud.aws.region.static=${AWS_REGION}
# aws.s3.bucket=${AWS_S3_BUCKET}

# ECPay Sandbox
# test param
# ??railway? , DonationService??url????
ecpay.merchant-id=${ECPAY_MERCHANT_ID}
ecpay.hash-key=${ECPAY_HASH_KEY}
ecpay.hash-iv=${ECPAY_HASH_IV}
ecpay.base-url=${ECPAY_BASE_URL}
ecpay.return-url=${ECPAY_RETURN_URL:https://47d6-111-248-156-24.ngrok-free.app/donate/ecpay-callback}
ecpay.client-back-url=${ECPAY_CLIENT_BACK_URL:https://talks-rust-tau.vercel.app/donate/success}
